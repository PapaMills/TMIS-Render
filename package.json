{
  "name": "nhs-tmis",
  "version": "1.0.0",
  "description": "NHS TMIS - Telemedicine Information System",
  "scripts": {
    "build": "cd src/frontend && npm install && npm run build",
    "start": "cd src/backend && npm install && npm start",
    "dev": "cd src/backend && npm run dev",
    "install-all": "npm install && cd src/backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {}
}
